import React from "react";

export const homePage=(history)=>{
    history.push('/')
}

export const peoplePage=(history)=>{
    history.push('/people')
}

export const photosPage=(history)=>{
    history.push('/photos')
}

export const profilePage=(history)=>{
    history.push('/profile')
}